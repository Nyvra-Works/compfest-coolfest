public enum CombatEventType
{
    // use enum flag (assign integer value of 2^n with n starting from 0 for the first element)
    BasicAttack = 1,
    HeadbuttAttack = 2,
    ThrowAttack = 4,
    DashAttack = 8  // contoh

}